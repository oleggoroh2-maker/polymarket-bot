# Feature Intelligence Engine v2.2

Shadow-mode analytics that combines factor separation, sample support, daily trend,
and historical stability. Adds `/features` and the `📊 Feature Intelligence` button.
Adaptive AI uses the reliability-adjusted values only for shadow proposals; live
alerts and production weights remain unchanged.
